language: objective-c
osx_image: xcode8.1
xcode_workspace: Jumblify.xcworkspace
xcode_scheme: JumblifyTests
xcode_sdk: iphonesimulator

install:
  - pod repo remove master && pod setup
  - pod install
